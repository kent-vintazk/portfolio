"use client";

// GSAP removed. Elements with [data-reveal*] attributes are now visible by default;
// components using them no longer have hidden initial states.
export default function ScrollAnimations() {
  return null;
}
